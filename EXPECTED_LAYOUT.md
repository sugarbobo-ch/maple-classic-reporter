# Maple Classic Reporter — UI / UX Layout 規範與設計需求

本文檔整理自 `對話紀錄.txt` 中討論並收斂的所有介面 Layout 期待與互動邏輯規範，作為後續前端 / GUI 開發與重構的標準參考依據。

---

## 1. 核心設計語言與風格 (Design System & Style)

- **設計風格**：以「新楓之谷：經典版」官網的像素冒險、羊皮紙與木框語言為視覺基底，轉譯成簡約、功能導向的桌面工具；不直接複製官網素材，也不犧牲資訊密度與操作效率。
- **主題色彩**：以 **楓之谷橘色 (Maple Orange)** 作為 Primary 互動色；楓紅、草綠與深藍僅用於明確的分類或狀態，不與主要操作競爭。
- **圖示系統**：採用 **Lucide Icons** 打包與封裝（自建 QIcon / SVG 載入模組，確保全站使用統一的 Lucide 圖示語言）。
- **按鈕與控制項規範 (Button System)**：
  - `Primary`：楓之谷橘底、深棕字（主要操作，如錄製影片、送出表單；不得以低對比白字呈現小型標籤）。
  - `Secondary`：淡橘底、深橘字（次要動作、候選 Tag/Chip 預設值）。
  - `Outline`：白底 / 透明底、灰色邊框（取消動作、一般輔助按鈕）。
  - `Danger`：紅色底/字（取消錄影、刪除動作）。
  - `Success`：綠色底/字（已連線狀態、白名單勾選完成）。
  - `Icon Button`：透明或淡色底方形按鈕 (40–44 px)，需搭配 Tooltip 說明。
  - 所有元件均具備明確的 `hover` / `pressed` / `focus` / `disabled` 狀態變化。

### 1.1 官網視覺基準與取樣來源

本節依 2026-08-16 實際檢視下列官方頁面所得，作為 Maple Classic Reporter 的視覺依據：

- [官網首頁](https://maplestoryclassic.beanfun.com/Main)：綜合公告、活動卡片與主視覺。
- [更新公告](https://maplestoryclassic.beanfun.com/Main?kind=759)：草綠色「更新」分類標籤。
- [重要公告](https://maplestoryclassic.beanfun.com/Main?kind=758)：深藍色「重要」分類標籤。

官網的核心印象是「暖色紙張內容面 + 木框容器 + 像素角色與自然場景」。桌面程式應採用其色彩、材質感與輪廓語言，不照搬大型宣傳圖、遊戲角色或官網背景圖。

### 1.2 官網原始色票 (Observed Palette)

以下色值取自官網實際渲染後的 computed styles，採用現有專案易於使用的 Hex 格式：

| Token | Hex | 官網用途 | Reporter 建議用途 |
| :--- | :--- | :--- | :--- |
| `maple-paper` | `#F9F0E7` | 頁面暖米色底 | 主視窗背景 |
| `maple-paper-raised` | `#F5EAD8` | 活動卡片底 | Card、設定群組、表單區塊 |
| `maple-wood-light` | `#C2A787` | 木框亮部 | 次級邊線、hover 裝飾 |
| `maple-wood-mid` | `#A38464` | 木材中間色 | 停用控制、頁碼與輔助裝飾 |
| `maple-gold-border` | `#C8A060` | 卡片與紙張邊框 | 預設 border、分隔線 |
| `maple-ink` | `#3A2010` | 深棕主要墨色 | 主文字、圖示、深色輪廓 |
| `maple-brown` | `#7B3A26` | 分類頁籤文字 | 次標題、未選取頁籤 |
| `maple-title` | `#814015` | 活動卡片標題 | Card title、重點標題 |
| `maple-muted` | `#696969` | 日期與次要資訊 | Secondary text、metadata |
| `maple-orange` | `#FF8711` | 目前頁籤與焦點 | Primary、selected、focus ring |
| `maple-red` | `#A32820` | 「活動」標籤 | 活動類別；Danger 另用專用 token |
| `maple-green` | `#4B7A24` | 「更新」標籤 | 成功、完成、已連線、更新類別 |
| `maple-blue` | `#154D85` | 「重要」標籤 | 重要資訊、資訊提示 |
| `maple-white` | `#FFFFFF` | 頂欄與標籤文字 | 深色背景上的文字與圖示 |

#### 建議語意 Token

```css
:root {
  --color-bg: #F9F0E7;
  --color-surface: #F5EAD8;
  --color-surface-hover: #FFF8EE;
  --color-text: #3A2010;
  --color-text-heading: #814015;
  --color-text-secondary: #696969;
  --color-border: #C8A060;
  --color-border-strong: #A38464;

  --color-primary: #FF8711;
  --color-primary-text: #3A2010;
  --color-category-event: #A32820;
  --color-status-success: #4B7A24;
  --color-status-important: #154D85;
  --color-on-dark: #FFFFFF;
}
```

- 保持「一色一義」：橘色代表主要互動或目前選取；紅色代表活動分類時，不可同時借作一般裝飾。破壞性操作應保留獨立的 `danger` token，避免與「活動」分類混淆。
- 同一個視圖原則上只有一個實心 Primary 按鈕；紅、綠、藍的實心底只用於狀態與分類標籤。
- `#FF8711` 與暖米底的 WCAG 2 對比約 `2.14:1`，僅可作大字、圖示、焦點框、選取底線或填色按鈕背景，不可直接作小型內文。Primary 按鈕文字優先使用 `#3A2010`，或經實際元件對比測試後採更深的橘色實作。
- 主文字 `#3A2010`、標題 `#814015`、次要文字 `#696969` 在 `#F9F0E7` 上的對比分別約為 `13.38:1`、`6.96:1`、`4.87:1`，適合各自的文字層級。

### 1.3 材質、輪廓與元件樣式

1. **背景與表面**
   - 主背景使用 `maple-paper`，可疊加極淡、低對比的紙張噪點或楓葉紋理；不得干擾文字閱讀。
   - 主要內容面使用 `maple-paper-raised`，以 1 px `maple-gold-border` 描邊區分層級。
   - 不使用玻璃擬態、強烈半透明或大面積冷灰；整體維持溫暖、實體紙材的感受。

2. **卡片與面板**
   - 官網活動卡片採約 `18 px` 圓角，Reporter 的主要 Card 建議收斂為 `12–16 px`；內部縮圖或預覽可與外框同圓角。
   - 設定群組、狀態面板與表單區塊可使用 `8–12 px` 圓角。
   - 預設不使用厚重陰影；以暖色邊框、1–2 px 深棕輪廓與表面明度差建立層次。浮起或拖曳狀態才使用柔和短陰影。
   - 木框感應透過雙層邊線或深淺棕色輪廓暗示，不使用寫實木紋圖片包圍每個小元件。

3. **標籤、Chip 與狀態**
   - 類別標籤參考官網的 `6 px` 圓角、實心底、粗體文字；高度依桌面密度調整為 `24–30 px`。
   - `活動` 使用 `maple-red`、`更新／成功` 使用 `maple-green`、`重要／資訊` 使用 `maple-blue`，文字皆為白色。
   - 一般建議 Chip 使用紙色底 + 金棕邊框；被選取後才切換為橘色輪廓或淡橘底。

4. **導覽、頁籤與選取狀態**
   - App Header 使用 `maple-paper` 或 `maple-paper-raised` 暖色表面、`maple-ink` 深棕文字與底部 `maple-gold-border` 分隔線；Logo／產品名在左，歷史與設定等全域動作在右。
   - 未選取頁籤使用 `maple-brown`；選取頁籤以 `maple-orange` 的文字、圖示或 2–3 px 底線表示，不必填滿整個頁籤。
   - 焦點狀態使用 2 px `maple-orange` 外框並保留足夠 offset，鍵盤操作時不可只靠顏色差異。

5. **字體與圖示**
   - 官網字體堆疊為 `Microsoft JhengHei`, `PingFang TC`, `Heiti TC`, `Noto Sans TC`, sans-serif；Reporter 依作業系統優先採用 `Microsoft JhengHei UI` / `Microsoft JhengHei`。
   - 主標題使用 700，內文使用 400–500；避免把像素字體用在長文或表單欄位。
   - Lucide 圖示維持單一線條系統；楓葉、木牌與像素風只作少量品牌裝飾，不取代可辨識的功能圖示。

6. **動態與互動回饋**
   - Hover：表面略提亮為 `surface-hover`，邊框轉為 `wood-mid`，位移最多 1 px。
   - Pressed：回到紙色或略微壓暗，不使用彈跳幅度過大的遊戲式動畫。
   - Loading：可借用官網白色圓點旋轉語言，但應置於半透明深色遮罩並提供可讀狀態文字。
   - 動畫時間以 `120–180 ms` 為主，並尊重系統的 reduced-motion 設定。

### 1.4 版面比例與視覺節奏

- 官網桌面內容以約 `1200 px` 置中欄呈現；Reporter 視窗則採流動式內容區，寬螢幕時限制主要表單與列表的可讀寬度，避免資訊橫跨全畫面。
- 官網以大型 Hero 與像素插圖建立情緒；Reporter 不設大型 Hero，改由 Header、核心操作卡與空狀態插圖承擔品牌感。
- 官網公告列表採「分類標籤／標題／日期」三欄閱讀節奏；歷史紀錄頁與辨識結果列表亦應沿用此清楚的左狀態、中內容、右時間結構。
- 主要間距採 8 px 基準：元件內距 `8 / 12 / 16 px`、區塊間距 `16 / 24 / 32 px`，避免因裝飾性素材破壞對齊。

---

## 2. 首頁 Layout 規範 (Main Window / Home Page)

首頁保持資訊簡潔、減少視覺負擔，由上至下分為 Header、Alert 警示區、快捷設定區 (Quick Settings)、四大核心動作卡片、快捷連結區 (Quick Links)、當前狀態列。

### ASCII Wireframe
```text
┌────────────────────────────────────────────────────────────────────────┐
│ Maple Classic Reporter  v1.2.1                         [歷史]  [設定]  │
├────────────────────────────────────────────────────────────────────────┤
│ ⚠ 尚未設定事證上傳目的地                                   [開始設定 →]│
├────────────────────────────────────────────────────────────────────────┤
│ 快捷設定                                                               │
│ 截圖視窗   [ 新楓之谷：經典版 (1920x1080)                            ▼ ] │
│ 音訊裝置   [ Realtek Digital Output                                 ▼ ] │
│ 錄影與回放 錄製 [ 15 ] 秒   FPS [ 30 ▼ ]   回放儲存 [ 30 ] 秒           │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  [ Icon ]    │  │  [ Icon ]    │  │  [ Icon ]    │  │  [ Icon ]    │ │
│  │   擷取圖片   │  │   錄製影片   │  │   循環錄影   │  │   選擇檔案   │ │
│  │   擷取畫面   │  │  錄製並辨識  │  │ 保留最近片段 │  │   本機事證   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│                       (Primary高權重)                                   │
├────────────────────────────────────────────────────────────────────────┤
│ 快捷連結                                                    (◂ 左右滑動 ▸)│
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ...   │
│ │  [Icon]  │ │  [Icon]  │ │  [Icon]  │ │  [Icon]  │ │  [Icon]  │       │
│ │ 新楓之谷 │ │ 外掛檢舉 │ │ 自訂連結 │ │ 自訂連結 │ │ 自訂連結 │       │
│ │ 官方網站 │ │ 官方頁面 │ │    ＋    │ │    ＋    │ │    ＋    │       │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├────────────────────────────────────────────────────────────────────────┤
│ ● 錄影 00:08    ◴ 倒數 3    ↻ 循環 00:30    新楓之谷：經典版       [⌃] │
└────────────────────────────────────────────────────────────────────────┘
```

### 區塊詳細規範
1. **Header (頂部列)**
   - **左側**：顯示應用程式標題 `Maple Classic Reporter` 與版本號 `v1.2.1`。
   - **右側**：`[歷史]` 與 `[設定]` 的 Icon-only 按鈕，hover 時顯示 Tooltip。

2. **Alert Section (條件警示區)**
   - 當尚未設定 Google Drive 或 Discord Webhook 上傳目的地時顯示。
   - 左側警示圖示與文字 `⚠ 尚未設定事證上傳目的地`，右側按鈕 `[開始設定 →]`（點擊跳轉至設定頁）。

3. **Quick Settings (快捷設定區)**
   - 位於警示區下方、四大核心動作卡片上方，提供常用參數的快速切換與調整：
     - **截圖／錄影視窗**（下拉選單）：切換當前捕捉的目標視窗（例如：`新楓之谷：經典版 (1920x1080)`）。
     - **音訊裝置**（下一行，下拉選單）：切換錄影與聲道捕捉的系統音訊裝置（例如：`Realtek Digital Output`）。
     - **錄製秒數與 FPS**：可快速微調單次錄影時長（秒）與幀率 `FPS`（下拉選單 30/60）。
     - **回放儲存秒數**：快速調整背景循環錄影 (Replay Buffer) 保留最近影像秒數。

4. **Core Action Cards (四大核心操作區)**
   - 四個等寬的卡片式按鈕：`[擷取圖片]`、`[錄製影片]`、`[循環錄影]`、`[選擇檔案]`。
   - 每張卡片包含 Icon、主要標題與簡短說明。
   - `[錄製影片]` 作為最核心動作，視覺權重最高（Primary Accent Highlight）。

5. **Quick Links (快捷連結區)**
   - 位於四大核心操作區下方、當前狀態列上方。
   - **預設與擴充卡片**：
     1. `新楓之谷：經典版官方網站`（預設 URL：`https://maplestoryclassic.beanfun.com/Main`）
     2. `外掛檢舉連結`（預設 URL：官方檢舉頁面）
     3. `自訂連結`
     4. `自訂連結`
     5. `自訂連結`
     6. `自訂連結`
   - **卡片樣式與視覺規範**：
     - **佈局**：Icon 與文字採**上下垂直排列**之卡片樣式。
     - **尺寸限制**：卡片限制最大寬度 (Max Width)，確保水平列排版精緻整齊。
     - **文字受壓處理**：文字過長或受到卡片寬度擠壓時，自動換行或截斷加 `...`（Ellipsis）。
     - **水平橫向捲動 (Horizontal Scroll)**：
       - 卡片容器支援水平橫向捲動。
       - **滾輪互動**：滑鼠游標位於快捷連結區塊上方時，滾動滑鼠滾輪即可進行左右橫向滑動（Mouse Wheel Horizontal Scroll）。

6. **Status Bar (當前狀態列 — 可折疊)**
   - **預設為折疊狀態 (Collapsed)**，簡潔顯示：
     - 錄影狀態 icon + 秒數（例：`● 錄影 00:08`）
     - 倒數 icon + 數字（例：`◴ 倒數 3`）
     - 循環錄影 icon + 緩衝秒数（例：`↻ 循環 00:30`）
     - 目前目標視窗名稱（字數過長時自動省略 `...`，Tooltip 顯示完整名稱）
     - 展開／收合按鈕 `[⌃ / ⌄]`
     - 無活動時顯示「待命」
   - **展開狀態 (Expanded)** 顯示詳細資訊與動作操作：
     ```text
     ├────────────────────────────────────────────────────────────────────────┤
     │ 當前狀態                                                           [⌄] │
     │                                                                        │
     │ 聲音來源     Realtek Digital Output                                    │
     │ 目標視窗     新楓之谷：經典版                                          │
     │ 視窗大小     1920 × 1080                                               │
     │ 錄影品質     1080p 30 FPS                                              │
     │                                                                        │
     │ ● 錄影中 00:08                                            [取消錄影]   │
     │ ↻ 循環緩衝 00:30                                    [儲存片段並辨識]   │
     └────────────────────────────────────────────────────────────────────────┘
     ```
   - **互動規則**：
     - 點擊錄影 Icon Button / 取消按鈕可取消錄影（Danger）。
     - 點擊循環錄影 Icon Button / 儲存按鈕可立即儲存最近片段並開始辨識。

---

## 3. 設定頁 Layout 規範 (Settings View)

採用 Sidebar 側邊欄分類與雙欄介面，移除傳統的「儲存設定」按鈕，改為即時自動保存。

### ASCII Wireframe

#### 視圖一：一般與偏好設定視圖 (General Settings View)
```text
┌────────────────────────────────────────────────────────────────────────┐
│ [←] 設定                                                               │
├───────────────┬────────────────────────────────────────────────────────┤
│ 一般          │ 上傳目的地                                             │
│ 上傳與帳號    │ Google Drive                       已連線   [重新驗證] │
│ 錄影與回放    │ Discord Webhook                    未設定     [設定]   │
│ 快捷鍵        │                                                        │
│ 快捷連結      │ 偏好設定                                               │
│ 表單預設      │ 自動刪除過期事證                                [Switch]│
│ 更新          │ 錄制系統聲音                                    [Switch]│
│ 關於          │ 預設伺服器                         [雪吉拉          ▼]│
│               │                                                        │
│               │ 白名單管理                                             │
│               │ [ player01 × ] [ player02 × ] [ 新增 ID...          ]  │
└───────────────┴────────────────────────────────────────────────────────┘
```

#### 視圖二：快捷連結管理視圖 (Quick Links Management View)
```text
┌────────────────────────────────────────────────────────────────────────┐
│ [←] 設定                                                               │
├───────────────┬────────────────────────────────────────────────────────┤
│ 一般          │ 快捷連結管理                                [ ＋ 新增連結 ]│
│ 上傳與帳號    │                                                        │
│ 錄影與回放    │  [≡] [Icon] 新楓之谷：經典版官方網站                    │
│ 快捷鍵        │             https://maplestoryclassic.beanfun.com...  │
│ 快捷連結      │             (預設連結)                         [編輯]  │
│ 表單預設      │                                                        │
│ 更新          │  [≡] [Icon] 外掛檢舉連結                               │
│ 關於          │             https://...                                │
│               │             (預設連結)                         [編輯]  │
│               │                                                        │
│               │  [≡] [Icon] 巴哈姆特討論區                             │
│               │             https://forum.gamer.com.tw/...             │
│               │             (自訂連結)                  [編輯] [刪除]  │
└───────────────┴────────────────────────────────────────────────────────┘
```

### 區塊與互動規範
1. **Header**：左側包含 `[←]` 返回按鈕，快速回到首頁。
2. **Sidebar 導覽**：側邊欄分區選單（一般、上傳與帳號、錄影與回放、快捷鍵、快捷連結、表單預設、更新、關於）。
3. **對齊與佈局規範**：
   - 設定項標題與說明文字統一靠左對齊（共享同一條左對齊線）。
   - 控制元件（Switch、下拉選單、按鈕、輸入框）統一靠右對齊（共享同一條右對齊線）。
   - Switch 開關固定放最右側。
4. **即時保存機制 (Auto-Save)**：
   - **移除「儲存設定」按鈕**。
   - Switch / 下拉選單：變更後立即自動保存。
   - 文字輸入框：使用者停止輸入約 `500 ms` 後自動保存。
   - 儲存成功不跳彈窗；失敗時顯示輕量提示。需重啟生效的項目顯示「重新啟動後生效」標示。
5. **快捷連結管理 (Quick Links Management)**：
   - **右上角操作**：提供 `[ ＋ 新增連結 ]` 按鈕，點擊彈出「新增快捷連結」對話框。
   - **排序機制**：左側包含拖曳／上下排序圖示 `[≡]`，可自訂首頁快捷連結卡片顯示順序。
   - **卡片條目包含內容**：
     - Icon 圖示預覽與 Icon 選擇器（支援 Lucide Icon 或預設圖示）。
     - 連結名稱標題（如 `新楓之谷：經典版官方網站`）。
     - 完整 URL 網址（自動驗證 `http://` 或 `https://` 前綴）。
     - 標籤型態（系統預設 / 使用者自訂）。
     - 右側操作按鈕：`[編輯]`（修改名稱、URL 或 Icon）、`[刪除]`（Danger，僅自訂連結可刪除）。
   - **新增 / 編輯對話框規範**：
     - 欄位包含：`連結名稱`、`目標網址 URL`、`選擇 Icon`。
     - 提供預覽卡片展示（即時預覽上下排列卡片與文字擠壓效果）。
     - 點擊 `[儲存]` 後即時更新並同步至首頁快捷連結區。
6. **表單預設 (Tag / Chip 型態)**：
   - 支援貼上逗號分隔文字（例：`player01, player02`），按下 Enter 或逗號自動建立 Chip。
   - 展示為 Tag/Chip 型態（如 `[player01 ×]`），點擊 `×` 移除，亦支援 `[Input][＋]` 與 `[－]` 增刪。
   - 自動去重與修剪前後空白。

---

## 4. 歷史紀錄頁 Layout 規範 (History View)

- Header 包含 `[←]` 返回按鈕與標題 `歷史紀錄`。
- 主區域使用表格 (Table View) 展示過去檢舉事證與上傳紀錄：
  - 欄位包含：時間、目標 ID、伺服器、地圖、上傳狀態、操作（開啟連結 / 查看詳情）。

---

## 5. 辨識進度與回報確認表單 Layout 規範 (Recognition & Report Form)

事證擷取或上傳後，分為「**辨識進度**」與「**回報確認表單**」兩個獨立階段，避免使用者一次面對過多欄位。

### 階段一：辨識進度階段 (Recognition Progress State)

展示 OCR 與媒體處理階段進度：

```text
┌────────────────────────────────────────────────────────────────────────┐
│ 正在分析事證...                                                        │
│                                                                        │
│ ● 讀取錄影片段                 完成                                    │
│ ● 擷取關鍵畫面                 完成                                    │
│ ◉ 辨識角色 ID                  處理中...                               │
│ ○ 辨識地圖名稱                 等待中                                  │
│ ○ 整理歷史與候選資料           等待中                                  │
│                                                                        │
│ 正在辨識第 3 / 8 張關鍵畫面...                                         │
│ [====================================>                       ] 37%     │
└────────────────────────────────────────────────────────────────────────┘
```
- 若辨識失敗，不直接關閉視窗，提示：「部分內容無法辨識，可以繼續手動填寫。」並提供 `[重新辨識]` 與 `[繼續填寫]` 按鈕。

---

### 階段二：回報確認表單階段 (Report Confirmation Form State)

單欄由上至下佈局，每個步驟使用橘色圓形數字標籤 (1~5)。

```text
┌────────────────────────────────────────────────────────────────────────┐
│ 1   確認錄製影片                                                       │
│     ┌────────────────────────────────────────────────────────────┐     │
│     │                      [ 簡易影片播放器 ]                    │     │
│     │ ►  00:05 / 00:15  ━━━━━●━━━━━━━━━━━━  🔊                     │     │
│     └────────────────────────────────────────────────────────────┘     │
│     [開啟檔案]  [開啟檔案位置]                                         │
├────────────────────────────────────────────────────────────────────────┤
│ 2   外掛玩家角色 ID                                                    │
│     [ Player01                                               ][ 貼上 ] │
│                                                                        │
│     OCR 辨識結果                                                       │
│     點選名稱即可帶入角色 ID。                                          │
│     [ Player01 ]  [ Player02 ]  [ Player03 ]            [ 管理白名單 ] │
├────────────────────────────────────────────────────────────────────────┤
│ 3   外掛角色所在伺服器                                                 │
│     (●) 雪吉拉          ( ) Other (伺服器名稱)                         │
├────────────────────────────────────────────────────────────────────────┤
│ 4   外掛角色所在地圖                                                   │
│     [ 地鐵一號線｜地區01                                             ] │
│     建議                                                               │
│     [ OCR：地鐵一號線｜地區01 ]  [ 自由市場 ]  [ 墮落城市 ]  [ 弓箭手村 ]│
├────────────────────────────────────────────────────────────────────────┤
│ 5   備註                                                               │
│     [ 自動打怪／疑似外掛行為                                 ][ ＋ ]   │
│     常用範本                                                           │
│     [ 自動打怪 ]  [ 外掛行為 ]  [ 長時間重複動作 ]                     │
├────────────────────────────────────────────────────────────────────────┤
│                                                      [取消]   [ 送出 ] │
└────────────────────────────────────────────────────────────────────────┘
```

#### 步驟欄位詳細規範：

1. **步驟 1：確認錄製影片**
   - 橘色圓形 `1` 標籤。
   - 內建簡易播放器（支援播放/暫停、時間軸、音量控制、檔名與時長展示）。若為圖片事證則改為圖片預覽。
   - 附帶 `[開啟檔案]` 與 `[開啟檔案位置]` 按鈕。

2. **步驟 2：外掛玩家角色 ID（含白名單管理雙模式）**
   - 橘色圓形 `2` 標籤。
   - Input 右側包含 `[貼上]` Icon 按鈕（直接讀取剪貼簿內文）。
   - 下方提供 Suggestions Chips（自動換行，無水平捲動）。
   - **一般模式 (Normal Mode)**：
     - 提示小字：`OCR 辨識結果：點選名稱即可帶入角色 ID。`
     - 點擊 Chip：名稱小說帶入 Input。
     - 與 Input 內容相同的 Chip 顯示為 **Primary (橘色)**，其餘為 **Secondary**。
     - 右側顯示 `[管理白名單]` (或 `[加入白名單]`) 按鈕。
   - **白名單模式 (Whitelist Mode - 按下管理白名單後切換)**：
     - 提示小字切換為：`選擇白名單：點選要排除的名稱；加入後，往後辨識將自動略過。`
     - 下方按鈕切換為 `[取消]` 與 `[完成]`。
     - 候選 Chips 切換為**可複選模式**，點擊切換勾選狀態（**不修改 Input**）。
     - 已選取的 Chip 顯示為 **Success (綠色) + 勾選 icon (`✓`)**。
     - 已存在白名單的 Chip 顯示為淡綠色「已加入」狀態，不可重複點選。
     - 點擊 `[完成]`：將勾選名稱加入白名單存檔，從 OCR 候選中移除；若當前 Input 在白名單中則自動清空並提示重新選擇。
     - 點擊 `[取消]`：放棄變更，恢復一般模式。

3. **步驟 3：外掛角色所在伺服器**
   - 橘色圓形 `3` 標籤。
   - Radio Button **水平左右排列**（例如 `(●) 雪吉拉   ( ) 其他伺服器`）。
   - 自動帶入設定中的預設伺服器，點選選鈕立即更新。

4. **步驟 4：外掛角色所在地圖**
   - 橘色圓形 `4` 標籤。
   - Input 自動帶入 OCR 第一高機率地圖名稱。
   - 下方 Suggestions Chips 最多顯示 5 個（自動換行）：
     - 第 1 個固定為 `[ OCR：地圖名 ]`
     - 第 2~5 個為歷史檢舉過的最近 5 個地圖（自動排除重複與 OCR 重複項，最新優先）。
   - 點擊 Chip 帶入 Input；與 Input 相符者顯示為 Primary。

5. **步驟 5：備註**
   - 橘色圓形 `5` 標籤。
   - Input 自動帶入預設範本。右側有 `[ ＋ ]` 按鈕，可將目前輸入內容直接加入常用範本。
   - 下方顯示常用範本 Chips（點擊帶入 Input，相符者顯示為 Primary）。

6. **底部操作區 (Footer Actions)**
   - `[取消]`：Outline / 文字按鈕。
   - `[送出]`：Primary 楓之谷橘色按鈕。
   - 送出時停用按鈕並顯示處理中狀態（防重複觸發），送出成功後顯示提示並返回首頁。

---

## 6. 總結 Layout 設計原則對照表

| 區塊 / 畫面 | 核心視覺風格 | 關鍵互動特點 |
| :--- | :--- | :--- |
| **全站通用 (System)** | 楓之谷橘色 Primary、**Lucide 打包圖示** | 按鈕 6 大階層 (Primary/Secondary/Outline/Danger/Success/Icon) |
| **首頁 (Main)** | 簡約桌面工具、橘色 Highlight | Alert 無目的地提醒、快捷設定區 (視窗/音訊/錄影/回放)、4大操作卡片、橫向滑動快捷連結卡片、可折疊當前狀態列 |
| **設定頁 (Settings)** | 側邊欄雙欄佈局、左右線對齊 | **無儲存按鈕（即時 Auto-save）**、表單預設 Tag/Chip 貼上即切分 |
| **歷史紀錄 (History)** | 表格化清單 (Table View) | 快速查看詳情與開啟線上事證連結 |
| **辨識進度 (Progress)** | 步驟清單 + 進度條 | 階段性反饋，異常時允許手動補全 |
| **回報表單 (Report Form)**| 橘色圓形數字 (1-5) 順序引導 | 影片播放器、**ID 雙模式 (選擇 vs 白名單)**、伺服器水平單選、歷史地圖 Top 5 Chips |
