# Python and local build output
.venv/
__pycache__/
*.py[cod]
dist/
build/
*.spec
!MapleClassicReporter.spec

# Local application data and evidence
data/recordings/
data/config/
*.dpapi

# OAuth credentials from older project layouts
client_secrets.json

# Release-only OAuth client resource (embedded into the release bundle, never committed)
build_secrets/
google_oauth_client.json
